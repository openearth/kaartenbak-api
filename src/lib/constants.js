const dateTypes = {
  creation: 'Creation',
  publication: 'Publication',
  revision: 'Revision',
}

const contentTypes = {
  xml: 'application/xml',
  json: 'application/json',
  html: 'text/html',
}

const resourceNotFound = 'resource_not_found'

module.exports = {
  dateTypes,
  contentTypes,
  resourceNotFound,
}
